<%= render "page/navbar" %>
<div class="oplata">
  <h1>Наши услуги</h1>
  <div class="container">
    <div class="row">
      <% @uslugis.each do |uslugis| %>
        <div class="col-md-4">
            <p><%= image_tag(uslugis.image_url, class: 'image3')%></p>
            <div class="title2"><%= uslugis.description %></div>
          </div>
      <%	end  %>
  </div>
 </div>
</div>
