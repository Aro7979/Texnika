<%= form_with(model: icone) do |form| %>
  <% if icone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(icone.errors.count, "error") %> prohibited this icone from being saved:</h2>

      <ul>
        <% icone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :image_url, style: "display: block" %>
    <%= form.text_field :image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
